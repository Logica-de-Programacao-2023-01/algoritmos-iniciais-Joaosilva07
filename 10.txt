leia n
leia lista
leia pesos
soma_notas = 0
soma_pesos = 0
para i de 0 até n-1 faça
	soma_notas = soma_notas + (lista * pesos)
	soma_pesos = soma_pesos + pesos

media_ponderada = soma_notas / soma_pesos
escreva ''A media ponderada e: '' + media_ponderada
------------------------------------------------------------------
leia lista
leia pesos
var resultado = 0
var somatorio_pesos  =  0
para cada elemento da lista faça:
	somatoria_elementos = somatoria_elementos + (somatoria_elementos * peso_correspondente)
para cada peso dos pesos faça:
	somatoria_pesos = somatoria_pesos + peso
resultado = somatorio_elementos / somatoria_pesos
escreva resultado
	
	
